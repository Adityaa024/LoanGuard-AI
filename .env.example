# Copy to .env and fill in. Synthetic data only — no real secrets in the repo.
ANTHROPIC_API_KEY=sk-ant-...
# Model used for vision + reasoning showcase moments.
HIVE_MODEL=claude-opus-4-8
PORT=8080
# Optional: set to 1 to disable live LLM calls and use deterministic fallbacks (offline/dev).
HIVE_OFFLINE=0
