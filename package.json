{
  "name": "loanguard-ai",
  "version": "2.4.0",
  "description": "LoanGuard-AI — Intelligent Loan Tape Securitization & Continuous Verification Copilot",
  "type": "module",
  "license": "Apache-2.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "seed": "node data/generate.js",
    "dev": "node src/server.js",
    "start": "node src/server.js",
    "build:web": "npm --prefix web install --include=dev && npm --prefix web run build",
    "build": "npm --prefix web install --include=dev && npm --prefix web run build",
    "postinstall": "node data/generate.js || true",
    "test": "node --test test/reconciliation_and_integrity.test.js && node scripts/test_all_csv_uploads.cjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "csv-parse": "^7.0.2",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.1.1",
    "sqlite": "^5.1.1",
    "sqlite3": "^5.1.7",
    "yaml": "^2.6.1",
    "zod": "^4.5.4"
  }
}
